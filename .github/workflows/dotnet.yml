name: .NET Library Pipeline

on:
  schedule:
    - cron: "0 * * * *"
  pull_request:
    
permissions:
  packages: write
  contents: write
  
jobs:
  dotnet-library:
    uses: ktsu-dev/workflows/.github/workflows/dotnet-library/pipeline.yml@main
    secrets: inherit
